version: "3.0"

services:
  rest-api:
    build: .
    volumes:
      - .:/api
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    command: "make run-dev"
    ports:
      - 8080:8000
  
     
  